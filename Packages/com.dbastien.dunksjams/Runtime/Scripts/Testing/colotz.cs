using UnityEngine;

public class colotz : MonoBehaviour
{
    [PaletteColor] public Color foo;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private void Start() { }

    // Update is called once per frame
    private void Update() { }
}